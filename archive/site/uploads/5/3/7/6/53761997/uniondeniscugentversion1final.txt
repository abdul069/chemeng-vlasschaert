#include<stdio.h>

int
main (void)
{
  int arr[20], i0, i, j, size;
  printf ("\nEnter array size : ");
  scanf ("%d", &size);
  printf ("\nAccept Numbers : ");
  for (i = 0; i < size; i++)
    scanf ("%d", &arr[i]);
  printf ("\nArray with Unique list  : ");
  i0 = 0;
  for (i = 0; i < size; i++)
    {
      j = 0;
      while (j < i && arr[j] != arr[i])
{
 j++;
}
      if (j >= i)
{
 if (i0 != i)
   arr[i0] = arr[i];
 i0++;
}
    }
  size = i0;
  for (i = 0; i < size; i++)
    {
      printf ("%d ", arr[i]);
    }

  return (0);